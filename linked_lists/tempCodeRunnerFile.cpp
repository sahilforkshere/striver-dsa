Node *reverseList(Node *head)
// {
//     Node *prev = nullptr;
//     Node *curr = head;
//     while (curr != nullptr)
//     {
//         Node *nxt = curr->next;
//         curr->next = prev;
//         prev = curr;
//         curr = nxt;
//     }
//     return prev;
// }